"use client";

// Composant à développer : interface de conversation voix/texte
// avec affichage des corrections après chaque message.

export default function ChatWindow() {
  return (
    <div>
      {/* TODO: liste des messages, input texte, bouton micro, affichage des corrections */}
      <p>Fenêtre de chat (à implémenter)</p>
    </div>
  );
}

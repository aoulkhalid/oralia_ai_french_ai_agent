# Dépendances de développement / test uniquement (pas nécessaires en
# production). Installer avec : pip install -r requirements-dev.txt
-r requirements.txt

pytest==8.3.3
pytest-cov==5.0.0
httpx==0.28.1
